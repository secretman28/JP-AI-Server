#!/usr/bin/env bash
set -euo pipefail
nvidia-smi --query-gpu=index,name,memory.total,temperature.gpu,power.draw --format=csv
f=/mnt/AI/temp/jp-benchmark.bin
dd if=/dev/zero of="$f" bs=1M count=2048 conv=fdatasync status=progress
dd if="$f" of=/dev/null bs=1M status=progress
rm -f "$f"
